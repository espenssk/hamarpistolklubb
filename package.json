{
  "name": "hps-homepage",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "SITE_URL=https://espenssk.github.io BASE_PATH=/hps-homepage/ npm run build && gh-pages -d dist -t true"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "allowScripts": {
    "esbuild": true
  }
}
